# AI-27 — Documentation and Runbooks

## Purpose

Create and update the engineering, security, runbook, and QA documentation for the SicherPlan AI Assistant.

This task documents the architecture, configuration, provider modes, knowledge ingestion, tool registry policy, tenant/permission enforcement, same-language behavior, testing approach, and operational procedures.

The documentation must be useful for future developers, Codex sessions, operators, QA reviewers, and security reviewers.

---

## Implementation Prompt for Codex

You are working in the SicherPlan repository.

Implement **Task AI-27 — Documentation and Runbooks** based on:

- `docs/sprint/AI-Assistant.md`
- all implemented assistant tasks `AI-01` through `AI-26`
- current repository conventions for documentation
- actual code and configuration, not assumptions

### First: inspect before writing

Before editing docs, inspect and report:

1. Existing docs folder structure.
2. Existing style conventions for engineering/security/runbook/QA docs.
3. Existing environment/config documentation style.
4. Existing runbook format.
5. Existing QA/test plan format.
6. Actual assistant module files and behavior.
7. Actual environment variable names implemented.
8. Actual provider modes implemented.
9. Actual test commands and test locations.
10. Any mismatch between the Sprint and implementation.

Do not write docs that claim functionality not implemented. Clearly mark future/follow-up features if mentioned.

---

## Required Documentation Files

Create or update the following files:

```text
docs/engineering/ai-assistant-architecture.md
docs/security/ai-assistant-security.md
docs/runbooks/ai-assistant-knowledge-reindex.md
docs/qa/ai-assistant-test-plan.md
```

If these folders do not exist, create them according to existing repository conventions.

If equivalent docs already exist, update them instead of duplicating.

---

# 1. `docs/engineering/ai-assistant-architecture.md`

## Required Sections

### 1. Overview

Explain that the AI Assistant is:

- embedded in the SicherPlan web platform;
- tenant-aware;
- role-aware;
- page-aware;
- multilingual / same-language;
- read-only in the first release;
- powered through backend provider abstraction;
- not a generic chatbot.

### 2. Architecture diagram

Include a text diagram:

```text
Web Assistant Widget
  -> Assistant API (/api/assistant/*)
  -> Assistant Service
  -> Auth Context / RBAC / Tenant Scope
  -> Knowledge Retrieval + Read-only Tools
  -> Provider Wrapper (mock/openai)
  -> Structured Assistant Response
```

### 3. Backend module structure

Document actual implemented files, for example:

```text
backend/app/modules/assistant/
  router.py
  schemas.py
  service.py
  repository.py
  policy.py
  language.py
  classifier.py
  prompt_builder.py
  provider.py
  openai_client.py
  knowledge/
  tools/
  diagnostics/
```

Use actual repository paths.

### 4. Frontend structure

Document actual components and store/API files, for example:

```text
web/apps/web-antd/src/components/sicherplan/assistant/
web/apps/web-antd/src/stores/assistant.ts
web/apps/web-antd/src/api/sicherplan/assistant.ts
```

Use actual paths.

### 5. Main backend flow

Document message handling:

```text
1. Receive message.
2. Load auth context.
3. Check feature flag and permission.
4. Load conversation.
5. Detect language.
6. Classify scope.
7. Retrieve knowledge if needed.
8. Build prompt.
9. Register allowed tools.
10. Call provider/mock.
11. Execute approved tools.
12. Audit tool calls.
13. Store messages.
14. Return structured response.
```

### 6. Data model

Document assistant tables:

- conversation;
- message;
- tool call;
- feedback;
- knowledge source;
- knowledge chunk;
- page route catalog;
- prompt/policy version if implemented.

Include key fields and purpose, not full migration dump.

### 7. API endpoints

Document actual endpoints:

```text
GET  /api/assistant/capabilities
POST /api/assistant/conversations
GET  /api/assistant/conversations/{conversation_id}
POST /api/assistant/conversations/{conversation_id}/messages
POST /api/assistant/conversations/{conversation_id}/feedback
GET  /api/assistant/knowledge/status
POST /api/assistant/knowledge/reindex
```

Only include implemented endpoints.

### 8. Structured response schema

Document the response shape:

- `conversation_id`;
- `message_id`;
- `detected_language`;
- `response_language`;
- `answer`;
- `scope`;
- `confidence`;
- `out_of_scope`;
- `diagnosis`;
- `links`;
- `missing_permissions`;
- `next_steps`;
- `tool_trace_id`.

### 9. Provider modes

Document:

- mock mode;
- OpenAI mode;
- disabled mode;
- how tests avoid real provider calls.

### 10. Knowledge retrieval

Document:

- source registration;
- hashing;
- chunking;
- retrieval;
- lexical fallback;
- vector support if implemented;
- what must not be indexed.

### 11. Tool registry

Document:

- how tools are registered;
- read-only classification;
- permissions;
- redaction;
- audit;
- how to add a new read-only diagnostic tool.

### 12. Same-language behavior

Document how language detection and response-language policy work.

### 13. Current diagnostic coverage

Document the first diagnostic:

- Employee shift visibility;
- Markus scenario;
- supported checks;
- limitations.

### 14. Follow-up roadmap

Mention future possibilities only as follow-up, not as implemented:

- portal-specific rollout;
- finance diagnostics;
- controlled write actions with confirmation;
- mobile assistant.

---

# 2. `docs/security/ai-assistant-security.md`

## Required Sections

### 1. Security model summary

Explain:

- backend-only provider calls;
- no direct browser-to-OpenAI calls;
- no arbitrary SQL;
- tool allowlist;
- read-only first release;
- tenant and permission enforcement;
- redaction;
- audit.

### 2. Tenant isolation

Document:

- tenant ID comes from auth context;
- user-provided tenant ID is ignored;
- tools must apply tenant/scope filters;
- inaccessible record existence must not be confirmed.

### 3. Permission enforcement

Document:

- `assistant.chat.access`;
- `assistant.diagnostics.read`;
- `assistant.feedback.write`;
- `assistant.knowledge.read`;
- `assistant.knowledge.reindex`;
- `assistant.admin`;
- actual role mapping if implemented.

### 4. Tool execution policy

Document:

- unknown tools rejected;
- write tools rejected;
- required permissions;
- scope behavior;
- input validation;
- redacted output;
- audit record.

### 5. Prompt injection protection

Document how the assistant handles:

- ignore-rules prompts;
- reveal-system-prompt requests;
- run-SQL requests;
- cross-tenant requests;
- HR-private/payroll requests;
- unauthorized links;
- write-action requests.

### 6. Data redaction

Document fields/categories that must not be exposed:

- tax identifiers;
- social insurance;
- health insurance;
- bank data;
- religion;
- payroll amounts where unauthorized;
- passwords;
- reset tokens;
- JWTs;
- API keys;
- secrets.

### 7. Provider privacy

Document implemented privacy settings:

- provider storage setting if implemented;
- minimal context;
- redacted tool outputs;
- error handling.

### 8. Audit

Document:

- what is audited;
- what is not stored raw;
- how to investigate tool calls;
- how denied/failed tool calls are represented.

### 9. Rate limiting and safeguards

Document:

- per-user limits;
- per-tenant limits;
- max input chars;
- max tool calls;
- max retrieved chunks;
- timeout behavior.

### 10. Security test coverage

List the backend security tests from `AI-25`.

---

# 3. `docs/runbooks/ai-assistant-knowledge-reindex.md`

## Required Sections

### 1. Purpose

Explain when and why knowledge reindex is needed.

### 2. When to reindex

Examples:

- docs changed;
- user manual updated;
- API export updated;
- page catalog changed;
- sprint docs updated;
- after deployment with new docs.

### 3. Who may reindex

Document required permission:

```text
assistant.knowledge.reindex
```

and actual role mapping if implemented.

### 4. Source locations

Document actual source configuration:

- repository docs;
- markdown files;
- test source path;
- uploaded docs only if the implementation supports it.

Do not claim large PDF ingestion works unless implemented.

### 5. Reindex methods

Document actual supported methods:

- API endpoint;
- CLI command;
- management script;
- admin UI if implemented.

Example if endpoint exists:

```bash
curl -X POST "$BASE_URL/api/assistant/knowledge/reindex" \
  -H "Authorization: Bearer <token>"
```

Use actual authentication/project style.

### 6. Expected output

Document expected counts:

- sources indexed;
- chunks created;
- unchanged sources skipped;
- failed sources;
- duration.

### 7. Validation after reindex

Document checks:

- `/api/assistant/knowledge/status`;
- test how-to question;
- logs;
- database rows.

### 8. Troubleshooting

Include:

- missing permission;
- feature disabled;
- source path missing;
- unsupported file type;
- embedding provider unavailable;
- vector extension unavailable;
- hash unchanged;
- provider timeout;
- malformed document.

### 9. Rollback / safe recovery

Document how to:

- mark failed source inactive;
- re-run indexing;
- clear test chunks if project supports it;
- fall back to lexical retrieval.

### 10. Operational cautions

- do not index operational data;
- do not index secrets;
- do not upload customer/employee private documents into global knowledge;
- avoid reindex during peak time if expensive.

---

# 4. `docs/qa/ai-assistant-test-plan.md`

## Required Sections

### 1. Purpose

Test plan for the AI Assistant baseline.

### 2. Test scope

Include:

- backend tests;
- frontend tests;
- security tests;
- language tests;
- knowledge tests;
- diagnostic tests;
- manual UAT.

### 3. Test environment

Document:

- mock provider mode;
- AI enabled flag;
- test database;
- seed permissions;
- no real OpenAI key needed for automated tests.

### 4. Backend automated tests

List implemented test files and what they cover.

### 5. Frontend automated tests

List implemented test files and what they cover.

### 6. Security tests

List key security tests:

- cross-tenant blocking;
- no inaccessible record existence leakage;
- prompt injection;
- no arbitrary SQL;
- no write tool;
- unauthorized links blocked;
- HR/payroll redaction;
- secrets not leaked;
- tool audit.

### 7. Same-language tests

Persian, German, English, and mixed-language behavior.

### 8. Markus diagnostic test matrix

Include table:

| Case | Expected Outcome |
|---|---|
| Shift not released | Blocking diagnosis |
| Missing employee access link | Blocking/limitation |
| Assignment cancelled | Blocking diagnosis |
| Approved absence | Blocking/warning |
| Missing qualification | Blocking/warning |
| Missing mobile permission | Missing permission response |
| Cross-tenant Markus | Safe not found/not permitted |
| Multiple Markus | Clarification |
| No Markus | Safe not found/not permitted |

### 9. Manual UAT checklist

Include scenarios:

- open widget after login;
- ask in Persian;
- ask in German;
- ask how-to question;
- ask unrelated question;
- ask Markus diagnostic;
- navigate while panel is open;
- click assistant link;
- submit feedback;
- use user without permission.

### 10. Non-regression checklist

Include:

- login;
- sessions;
- health endpoints;
- routes;
- IAM;
- customers;
- employees;
- planning;
- field;
- finance;
- reporting;
- portals.

### 11. Commands

Document actual test commands.

### 12. Known limitations and follow-ups

Document honest current limitations.

---

## Documentation Rules

1. Do not document unimplemented features as complete.
2. Clearly mark future items as follow-up.
3. Use actual file paths and actual environment variable names.
4. Keep docs readable for both developers and operators.
5. Do not include secrets or real keys.
6. Do not include raw production data.
7. Do not duplicate the full Sprint; summarize and link/reference it.
8. Keep security guidance explicit and strict.
9. Add examples where useful.
10. Keep terminology consistent: Tenant, RBAC, Assignment, Shift Plan, Employee Self-Service, Watchbook, Actual Record.

---

## Required Commands

Run relevant formatting/lint/docs checks if the project has them:

```bash
markdownlint docs || true
prettier --check docs || true
```

Also run assistant tests if documentation references test commands:

```bash
cd backend
pytest tests/modules/assistant -q
```

and frontend test commands if available.

Report actual commands and results.

---

## Deliverables

1. `docs/engineering/ai-assistant-architecture.md`
2. `docs/security/ai-assistant-security.md`
3. `docs/runbooks/ai-assistant-knowledge-reindex.md`
4. `docs/qa/ai-assistant-test-plan.md`
5. Summary of documented implemented behavior.
6. List of documented follow-up items.

---

## Acceptance Criteria

This task is complete when:

- all required docs exist;
- docs reflect actual implementation;
- docs explain provider modes and config;
- docs explain knowledge reindex;
- docs explain tool registry and how to add tools;
- docs explain tenant/permission enforcement;
- docs explain same-language behavior;
- docs explain prompt injection and redaction policy;
- docs list test coverage and commands;
- docs do not claim unimplemented features as done;
- docs contain no secrets or production data.

---

# Review Prompt for Task AI-27

Review the implementation of **AI-27 — Documentation and Runbooks**.

You are reviewing for accuracy, completeness, security clarity, and operational usefulness.

## Review Checklist

### Required files

Verify these exist:

```text
docs/engineering/ai-assistant-architecture.md
docs/security/ai-assistant-security.md
docs/runbooks/ai-assistant-knowledge-reindex.md
docs/qa/ai-assistant-test-plan.md
```

### Accuracy

- Do docs match actual implemented file paths?
- Do docs use actual environment variable names?
- Do docs list only implemented endpoints?
- Do docs describe actual provider modes?
- Do docs describe actual test commands?
- Are future features clearly marked as follow-up?
- Are there no false claims?

### Architecture doc

Verify it explains:

- global assistant architecture;
- backend module structure;
- frontend structure;
- message flow;
- data model;
- API endpoints;
- structured response schema;
- provider modes;
- knowledge retrieval;
- tool registry;
- same-language behavior;
- current diagnostic coverage.

### Security doc

Verify it explains:

- backend-only provider calls;
- no direct browser-to-OpenAI calls;
- no arbitrary SQL;
- read-only first release;
- tenant isolation;
- permission enforcement;
- no inaccessible record existence leakage;
- prompt injection protection;
- redaction;
- provider privacy;
- audit;
- rate limiting;
- security tests.

### Knowledge reindex runbook

Verify it explains:

- when to reindex;
- who can reindex;
- how to reindex;
- expected output;
- validation;
- troubleshooting;
- fallback behavior;
- operational cautions.

### QA test plan

Verify it includes:

- backend tests;
- frontend tests;
- security tests;
- same-language tests;
- Markus matrix;
- manual UAT checklist;
- non-regression checklist;
- actual commands;
- known limitations.

### Security and privacy

- No secrets included.
- No production data included.
- No real API keys included.
- No sensitive example data beyond safe fictional examples.

## Required Review Output

Return:

```text
Result: PASS / PASS WITH MINOR ISSUES / FAIL

Documentation Summary:
- ...

Accuracy Findings:
- ...

Security Documentation Findings:
- ...

Missing Sections:
- ...

Required Fixes:
- ...

Recommended Follow-ups:
- ...
```

If any required document is missing, mark the review as **FAIL**.
