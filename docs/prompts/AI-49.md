# AI-49 — Page Help Coverage Expansion for Core Workspaces C-01, P-02, P-03, P-04, P-05, PS-01

## Purpose

The employee creation answer is stronger because `E-01` has verified page-help/UI action facts. Customer and planning answers remain weak because other core pages do not appear to have equivalent page-help coverage.

This task expands page-help and UI/action facts for the core workspaces required for English/German RAG answers.

This is not a canned-answer task. Page-help facts are grounding sources only.

---

## Prompt for Codex

You are working on the SicherPlan repository.

Implement **AI-49 — Page Help Coverage Expansion for Core Workspaces**.

### Step 1 — Inspect Existing Page Help

Inspect:

```text
backend/app/modules/assistant/page_help_seed.py
backend/app/modules/assistant/page_catalog_seed.py
web/apps/web-antd/src/router/routes/modules/*
web/apps/web-antd/src/views or pages for customers/planning/platform services
web/apps/web-antd/src/components/sicherplan/*
```

Report:

1. Which pages have page-help manifests.
2. Which pages have verified actions.
3. Which actions have exact frontend labels.
4. Which labels are unverified.
5. Whether frontend components have `data-testid` or `data-assistant-action`.

### Step 2 — Required Page Help Coverage

Add page-help facts for:

```text
C-01 — Customers Workspace
P-02 — Orders & Planning Records
P-03 — Shift Planning
P-04 — Staffing Board & Coverage
P-05 — Dispatch, Outputs & Subcontractor Releases
PS-01 — Platform Services Workspace
```

For each page, include:

```json
{
  "page_id": "C-01",
  "page_title_en": "Customers Workspace",
  "page_title_de": "Kundenverwaltung / Customers Workspace",
  "module_key": "customers",
  "purpose_en": "...",
  "purpose_de": "...",
  "sidebar_path_en": [],
  "sidebar_path_de": [],
  "actions": [],
  "form_sections": [],
  "workflow_keys": [],
  "api_families": [],
  "source_basis": []
}
```

### Step 3 — C-01 Customers Workspace

Include facts:

- customer registry/detail;
- create/update customer master;
- contacts;
- addresses;
- billing profile;
- invoice parties;
- rate cards/rate lines;
- surcharge rules;
- history/attachments;
- portal privacy/login history;
- employee blocks;
- import/export/vCard.

If exact button label is verified in frontend, include it. If not, include:

```json
{
  "action_key": "customer.create.open",
  "label_status": "unverified",
  "purpose": "Open the customer creation form",
  "required_permissions": ["customers.create"]
}
```

### Step 4 — P-02 Orders & Planning Records

Include facts:

- create customer order/project;
- equipment lines;
- requirement lines;
- attachments;
- commercial link;
- planning record;
- release state;
- order-to-planning relationship.

### Step 5 — P-03 Shift Planning

Include facts:

- shift templates;
- shift plans;
- shift series;
- concrete shifts;
- release diagnostics;
- visibility flags;
- outputs/copy/generation.

### Step 6 — P-04 Staffing Board & Coverage

Include facts:

- board shifts;
- demand groups;
- teams;
- assignments;
- assignment validations;
- validation overrides;
- assign/unassign/substitute;
- staffing coverage.

### Step 7 — P-05 Dispatch and Releases

Include facts:

- shift outputs;
- dispatch preview/messages;
- subcontractor releases;
- import dry-run/execute;
- release/visibility downstream context.

### Step 8 — PS-01 Platform Services

Include facts:

- documents;
- document versions;
- document links;
- communication templates;
- outbound messages;
- notices;
- integration endpoints/jobs/outbox.

### Step 9 — Frontend Action Markers

If practical, add non-invasive frontend markers for key actions:

```html
data-assistant-action="customer.create.open"
data-testid="customer-create-button"
```

Do not break UI behavior.

If exact UI action cannot be verified, do not invent it. Mark it unverified and let OpenAI explain process without exact button label.

### Step 10 — Tests

Add:

```text
backend/tests/modules/assistant/test_page_help_core_workspaces.py
backend/tests/modules/assistant/test_page_help_verified_vs_unverified_labels.py
frontend tests if data-assistant-action markers are added
```

Verify:
- page-help exists for all required pages;
- facts are content-bearing;
- source basis exists;
- unverified labels are not treated as verified;
- RAG retrieval can include page-help facts for C-01 and P-02.

### Acceptance Criteria

This task is complete only when:

1. `C-01`, `P-02`, `P-03`, `P-04`, `P-05`, `PS-01` all have page-help facts.
2. Exact UI labels are used only when verified.
3. Unverified actions are clearly marked.
4. Page-help facts are retrievable by RAG.
5. Tests pass.
6. No canned final answers are introduced.

---

## Review Checklist for Codex

After implementation, report:

1. Page-help coverage matrix.
2. Verified actions vs unverified actions.
3. Frontend markers added, if any.
4. Tests added and results.
5. Remaining UI coverage gaps.
